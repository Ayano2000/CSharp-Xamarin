<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:local="clr-namespace:WeatherTracker.ViewModels"
             mc:Ignorable="d"
             x:Class="WeatherTracker.WeatherInfo">

   
    
    <ContentView.Content>
        <StackLayout>
            <ScrollView >
                <StackLayout Padding="10" Orientation="{Binding StackLayoutOrientation}" BindableLayout.ItemsSource="{Binding CityWeatherViewModels}">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <StackLayout>
                                <Label Text="Location"/>
                                <Label Text="{Binding Location}" 
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Temperature"/>
                                <Label Text="{Binding Temperature}" 
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Wind Speed"/>
                                <Label Text="{Binding WindSpeed}" 
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Humidity"/>
                                <Label Text="{Binding Humidity}" 
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Visibility"/>
                                <Label Text="{Binding Visibility}" 
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Time of Sunrise"/>
                                <Label Text="{Binding Sunrise, Converter={StaticResource LongToDateTimeConverter}}"
                                       Style="{StaticResource labelResultStyle}" />
                                <Label Text="Time of Sunset"/>
                                <Label Text="{Binding Sunset, Converter={StaticResource LongToDateTimeConverter}}" 
                                       Style="{StaticResource labelResultStyle}" />
                            </StackLayout>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </StackLayout>
            </ScrollView>
        </StackLayout>
    </ContentView.Content>
    
</ContentView>